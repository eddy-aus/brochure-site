import BEM from './bem';
import isHiRes from './is-hi-res';
import toRange from './to-range';
import useEventListenter from './use-event-listenter';

const utils = {
  BEM,
  isHiRes,
  toRange,
  useEventListenter,
};

export { BEM, isHiRes, toRange, useEventListenter, utils as default };
