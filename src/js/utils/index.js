import BEM from './bem';
import toRange from './to-range';
import useEventListenter from './use-event-listenter';

const utils = {
  BEM,
  toRange,
  useEventListenter,
};

export { BEM, toRange, useEventListenter, utils as default };
