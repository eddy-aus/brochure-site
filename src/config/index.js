import address from './address';
import namespace from './namespace';
import organisation from './organisation';
import pages from './pages';

const config = {
  address,
  namespace,
  organisation,
  pages,
};

export { address, config as default, namespace, organisation, pages };
